server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations
#  cloud:
#    config:
#      server:
#        git:
#          uri: file:///${user.home}/Desktop/MicroServices/config-server/src/main/resources/configurations
